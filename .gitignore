node_modules
dist
.env
.env.local
*.log

# Manual J4U capture file downloaded from your browser (may hold session-derived
# data). Never commit — import it with `npm run j4u:import`, then it can be deleted.
j4u-capture.json
